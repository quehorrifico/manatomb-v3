{{ define "rules_home" }}
  {{ template "layout_header" . }}

  <div class="mt-static-page mt-static-page--message">
    <section class="mt-static-message" aria-labelledby="rules-title">
      <h1 id="rules-title" class="mt-static-title">Rules reference is coming soon.</h1>
      <p class="mt-static-lede">Card search and deck playtesting remain available while the reference is being prepared.</p>
    </section>
  </div>

  {{ template "layout_footer" . }}
{{ end }}
